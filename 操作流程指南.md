# HideWG 操作流程指南

HideWG 在 WireGuard UDP 报文外增加一层加密隐藏传输层，消除固定头部、固定长度、握手时序和 keepalive 模式等可被流量分析工具识别的显式特征。整个系统不需要修改 WireGuard 核心协议。

## 架构概览

```
WireGuard 程序
    ↓ 加密后的 UDP 包
HideWG 内层代理 (inner, 如 127.0.0.1:51830)
    ↓ ChaCha20Poly1305 AEAD 编码 + 填充 + 分片 + 碎片随机化
HideWG 外层代理 (outer, 如 0.0.0.0:55820)
    ↓ UDP / TLS / WebSocket-over-TLS 网络传输
对端 HideWG 外层代理 (outer, 如 peer:55821)
    ↓ AEAD 验证 + 重组 + 解码
对端 HideWG 内层代理 (inner, 如 127.0.0.1:51831)
    ↓ 还原原始 UDP 包
对端 WireGuard 程序
```

## 前置要求

- Python 3.10+
- `cryptography` 库（ChaCha20Poly1305 AEAD 加密，`pip install cryptography`）
- 可选：WireGuard（仅真实隧道部署阶段需要）

---

## 第一阶段：本地验证（无需 WireGuard）

本地验证使用模拟的 WireGuard 形态 UDP 报文，不需要管理员权限，也不需要安装 WireGuard。

### 1.1 运行验证套件

**Windows PowerShell：**

```powershell
.\hidewg.cmd verify --config configs/lab.yaml --output artifacts
```

**Linux / macOS：**

```bash
./hidewg verify --config configs/lab.yaml --output artifacts
```

验证会自动执行以下三类测试：

| 测试类别 | 内容 |
|---------|------|
| 功能测试 | 基本往返、文件完整性(SHA256)、UDP 流、大包分片乱序重组、异常包处理(重放/篡改/畸形)、状态可观测性、代理回环 |
| 隐蔽性测试 | 规则检测对比（原始 WireGuard vs HideWG）、统计分类器检测 |
| 性能测试 | 吞吐保留率、延迟增量、带宽膨胀比、CPU 占用、内存峰值、丢包容忍度(1%/3%/5%)、并发稳定性 |

### 1.2 查看验证结果

验证通过后，所有产物位于 `artifacts/` 目录：

```
artifacts/
├── verify_report.json        # 总报告（passed: true/false）
├── stealth_report.json       # 隐蔽性详细报告
├── performance_report.csv    # 性能指标 CSV
├── runtime_log.jsonl         # 运行时事件日志
├── capture.pcap              # HideWG 封装后流量（可用 Wireshark 打开）
├── raw_wireguard.pcap        # 原始 WireGuard 流量对比
└── state.json                # 最终运行状态快照
```

快速查看通过情况：

```bash
python -c "import json; r=json.load(open('artifacts/verify_report.json')); print('通过' if r['passed'] else '未通过')"
```

### 1.3 关键指标解读

从 `verify_report.json` 的 `summary` 字段可以查看：

- `raw_rule_hit_rate`：原始 WireGuard 流量的规则命中率（越高越容易被识别）
- `hidewg_rule_hit_rate`：HideWG 封装后的规则命中率（越低说明隐藏效果越好）
- `classifier_accuracy`：统计分类器对三类流量(raw/hidewg/control)的分类准确率
- `throughput_retention`：吞吐量保留率（HideWG 吞吐 / 基线吞吐）
- `bandwidth_expansion`：带宽膨胀比（外层字节 / 内层字节，>1 表示有开销）

---

## 第二阶段：真实 WireGuard 部署

### 2.1 网络拓扑

以两台机器为例（也可以在同一台机器上用不同端口测试）：

```
客户端机器                          服务器机器
┌─────────────────┐              ┌─────────────────┐
│ WireGuard Client│              │ WireGuard Server│
│ 端口 51820      │              │ 端口 51821      │
│   ↓ 本地回环    │              │   ↓ 本地回环    │
│ HideWG Client   │    UDP      │ HideWG Server   │
│ inner: 51830    │◄────────────►│ inner: 51831    │
│ outer: 55820    │              │ outer: 55821    │
└─────────────────┘              └─────────────────┘
       客户端公网 IP                    服务器公网 IP
```

数据流向（以 ping 为例）：

```
客户端 ping 10.66.0.1
  → WireGuard 加密为 UDP 包，发往 127.0.0.1:51830
  → HideWG Client 内层收到，编码+填充+分片
  → HideWG Client 外层发出，经 UDP 发往服务器公网 IP:55821
  → 互联网传输（外人看到的是随机字节的 UDP 包）
  → HideWG Server 外层收到，验证+重组+解码
  → HideWG Server 内层还原原始 WireGuard 包，发往 127.0.0.1:51821
  → WireGuard Server 解密，处理 ping，原路返回
```

---

### 2.2 服务端环境准备（以 Ubuntu 22.04 为例）

#### 2.2.1 系统要求

| 项目 | 要求 |
|------|------|
| OS | Ubuntu 20.04+ / Debian 11+ / CentOS 8+ |
| Python | 3.10+（需 pip install cryptography） |
| WireGuard | 内核模块或 userspace 实现 |
| 内存 | ≥ 256MB 可用 |
| 网络 | 有公网 IP，UDP 端口可对外通信 |

#### 2.2.2 安装 WireGuard

```bash
# Ubuntu / Debian
sudo apt update
sudo apt install -y wireguard

# CentOS / RHEL
sudo dnf install -y epel-release
sudo dnf install -y wireguard-tools

# 验证安装
wg --version
```

#### 2.2.3 安装 Python + 依赖

```bash
# 检查当前版本
python3 --version

# Ubuntu（如果版本低于 3.10）
sudo apt install -y python3.12

# CentOS
sudo dnf install -y python3.12

# 创建软链接（可选，方便后续直接用 python）
sudo ln -sf $(which python3.12) /usr/local/bin/python3

# 安装 cryptography 库（ChaCha20Poly1305 AEAD 加密）
pip3 install cryptography
```

#### 2.2.4 上传 HideWG 代码

```bash
# 方法一：从本地 scp 上传
scp -r E:\safewg\hidewg_app E:\safewg\hidewg E:\safewg\configs root@服务器IP:/opt/hidewg/

# 方法二：git clone（如果已推送到远程仓库）
cd /opt
git clone https://github.com/你的仓库/safewg.git hidewg

# 设置执行权限
chmod +x /opt/hidewg/hidewg
```

#### 2.2.5 生成 WireGuard 密钥对

```bash
# 生成服务端密钥对
wg genkey | tee /etc/wireguard/server_private.key | wg pubkey > /etc/wireguard/server_public.key
chmod 600 /etc/wireguard/server_private.key

# 生成客户端密钥对
wg genkey | tee /etc/wireguard/client_private.key | wg pubkey > /etc/wireguard/client_public.key
chmod 600 /etc/wireguard/client_private.key

# 查看密钥（后续配置需要用到）
echo "Server Private: $(cat /etc/wireguard/server_private.key)"
echo "Server Public:  $(cat /etc/wireguard/server_public.key)"
echo "Client Private: $(cat /etc/wireguard/client_private.key)"
echo "Client Public:  $(cat /etc/wireguard/client_public.key)"
```

将输出的四个密钥值记下来，后面填入配置文件。

#### 2.2.6 配置防火墙

云服务器有**两层防火墙**，都需要放通，缺一不可：

```
客户端 UDP 包
  → 第一层：云安全组（阿里云/腾讯云/AWS 控制台）  ← 必须放通
  → 第二层：OS 防火墙（ufw / iptables）           ← 必须放通
  → HideWG 进程
```

需要放通的端口：

| 端口 | 协议 | 方向 | 放通位置 | 用途 |
|------|------|------|---------|------|
| 55821 | UDP | 入站 | **云安全组 + OS 防火墙** | HideWG 外层代理 |
| 22 | TCP | 入站 | 云安全组 + OS 防火墙 | SSH 管理 |
| 51821 | UDP | 本机回环 | 无需开放 | WireGuard → HideWG 内层 |

**第一步：配置云安全组**

以阿里云为例（腾讯云/AWS 操作类似）：

1. 登录云控制台 → ECS → 安全组
2. 找到服务器所在的安全组 → 入方向规则 → 添加规则
3. 添加以下规则：

| 授权策略 | 优先级 | 协议类型 | 端口范围 | 授权对象 |
|---------|-------|---------|---------|---------|
| 允许 | 1 | 自定义 UDP | 55821/55821 | 客户端公网 IP/32 |
| 允许 | 1 | 自定义 TCP | 22/22 | 0.0.0.0/0 |

> **安全建议**：HideWG 端口的授权对象填客户端的具体公网 IP（如 `1.2.3.4/32`），不要填 `0.0.0.0/0`。这样只有你的客户端能连到 HideWG 外层端口。

**第二步：配置 OS 防火墙**

```bash
# === UFW（Ubuntu 默认）===

# 放通 HideWG 外层端口
sudo ufw allow 55821/udp comment "HideWG outer"

# 放通 SSH（防止锁在外面）
sudo ufw allow 22/tcp

# 启用防火墙
sudo ufw enable

# 查看规则
sudo ufw status verbose


# === iptables（CentOS 或无 UFW 时）===

# 放通 HideWG 外层端口
sudo iptables -A INPUT -p udp --dport 55821 -j ACCEPT

# 保存规则（CentOS）
sudo iptables-save > /etc/sysconfig/iptables

# 或使用 firewalld（CentOS 8+）
sudo firewall-cmd --permanent --add-port=55821/udp
sudo firewall-cmd --reload
```

> **注意**：不要开放 WireGuard 的 51821 端口到公网。WireGuard 的 Endpoint 已指向 `127.0.0.1:51831`，只接受来自本地 HideWG 代理的连接。

---

### 2.3 服务端配置

#### 2.3.1 WireGuard 配置

```bash
sudo vim /etc/wireguard/wg0.conf
```

写入以下内容（替换尖括号中的值）：

```ini
[Interface]
PrivateKey = <server_private.key 的内容>
Address = 10.66.0.1/24
ListenPort = 51821
# 启用 IP 转发（如果需要让客户端通过 VPN 上网）
PostUp = sysctl -w net.ipv4.ip_forward=1

[Peer]
PublicKey = <client_public.key 的内容>
AllowedIPs = 10.66.0.2/32
Endpoint = 127.0.0.1:51831
```

> **Endpoint 是 `127.0.0.1:51831`** 而不是公网 IP。WireGuard 的 UDP 包先发给本地 HideWG 内层代理，由 HideWG 转发到对端。

#### 2.3.2 HideWG 配置

```bash
cd /opt/hidewg
vim configs/server.yaml
```

完整配置：

```yaml
# HideWG 服务端配置
# 点击 WireGuard Endpoint 指向 127.0.0.1:51831
shared_secret: <自定义密钥，两端一致，如 MySecretKey2026>
session_id: 0x48445747

# 内层：面向本地 WireGuard
inner_listen_host: 127.0.0.1
inner_listen_port: 51831

# 外层：面向互联网
outer_listen_host: 0.0.0.0
outer_listen_port: 55821
# 客户端在 NAT 后面，启动后会自动学习客户端的真实地址
peer_host: 127.0.0.1
peer_port: 55820

# 本地 WireGuard
wireguard_host: 127.0.0.1
wireguard_port: 51821

# 碎片化 + 填充
max_fragment_payload: 1300
padding_buckets: [96, 128, 192, 256, 384, 512, 768, 1024, 1280]
padding_jitter_chance: 0.20
timing_jitter_ms: 2.5

# ===== 反检测填充策略 =====
padding_mode: adaptive
adaptive_padding: true
adapt_window_size: 128
adapt_strength: 0.6
bucket_jitter_sigma: 15.0
burst_timing: true

# AmneziaWG 借鉴
junk_count: 3
junk_min_size: 64
junk_max_size: 384
noise_prefix_min: 0
noise_prefix_max: 24
s4_padding_max: 48

cover_traffic: true
cover_rate_hz: 0.5
cover_burst_max: 2
cover_bidirectional: true
bucket_drift: 8

# 传输层（默认 UDP）
outer_transport: udp

# 使用 WebSocket-over-TLS 时取消注释:
# outer_transport: ws
# outer_listen_port: 443
# tls_certfile: .hidewg/tls/server.pem
# tls_keyfile: .hidewg/tls/server-key.pem

# 状态输出
state_path: .hidewg/server_state.json
log_path: .hidewg/server_runtime.jsonl
pid_path: .hidewg/server.pid
```

---

### 2.4 客户端配置（Windows）

#### 2.4.1 安装 WireGuard

1. 访问 [WireGuard 官网](https://www.wireguard.com/install/) 下载 Windows 客户端
2. 安装后打开 WireGuard，点击左下角 **"新建空隧道"**

#### 2.4.2 WireGuard 配置

在 WireGuard 客户端中，清空模板内容，粘贴：

```ini
[Interface]
PrivateKey = <client_private.key 的内容>
Address = 10.66.0.2/32
ListenPort = 51820
MTU = 1280

[Peer]
PublicKey = <server_public.key 的内容>
AllowedIPs = 10.66.0.1/32
Endpoint = 127.0.0.1:51830
PersistentKeepalive = 25
```

保存并命名为 `HideWG-VPN`。**先不要点击连接。**

> **Endpoint 是 `127.0.0.1:51830`**——指向本地 HideWG 内层代理。

#### 2.4.3 HideWG 配置

编辑 `configs/client.yaml`：

```yaml
# HideWG 客户端配置
# WireGuard Endpoint 指向 127.0.0.1:51830
shared_secret: <与服务端相同的密钥>
session_id: 0x48445747

# 内层：面向本地 WireGuard
inner_listen_host: 127.0.0.1
inner_listen_port: 51830

# 外层：面向互联网
outer_listen_host: 0.0.0.0
outer_listen_port: 55820
peer_host: <服务器公网 IP>
peer_port: 55821

# 本地 WireGuard
wireguard_host: 127.0.0.1
wireguard_port: 51820

# 碎片化 + 填充（与服务端一致）
max_fragment_payload: 1300
padding_buckets: [96, 128, 192, 256, 384, 512, 768, 1024, 1280]
padding_jitter_chance: 0.20
timing_jitter_ms: 2.5

# ===== 反检测填充策略（与服务端一致）=====
padding_mode: adaptive
adaptive_padding: true
adapt_window_size: 128
adapt_strength: 0.6
bucket_jitter_sigma: 15.0
burst_timing: true

# AmneziaWG 借鉴
junk_count: 3
junk_min_size: 64
junk_max_size: 384
noise_prefix_min: 0
noise_prefix_max: 24
s4_padding_max: 48

cover_traffic: true
cover_rate_hz: 0.5
cover_burst_max: 2
cover_bidirectional: true
bucket_drift: 8

# 传输层（默认 UDP，与服务端一致）
outer_transport: udp

# 使用 WebSocket-over-TLS 时取消注释:
# outer_transport: ws
# peer_port: 443
# ws_path: /ws
# tls_ca_certs: .hidewg/tls/server.pem

# 状态输出
state_path: .hidewg/client_state.json
log_path: .hidewg/client_runtime.jsonl
pid_path: .hidewg/client.pid
```

---

### 2.5 启动顺序（严格遵守）

启动和停止有严格顺序要求，违反顺序会导致 WireGuard 握手失败。

#### 启动流程

```
步骤 1    服务端启动 HideWG 代理
  ↓
步骤 2    客户端启动 HideWG 代理
  ↓
步骤 3    服务端启动 WireGuard
  ↓
步骤 4    客户端激活 WireGuard 隧道
  ↓
步骤 5    验证连通性
```

**步骤 1 — 服务端启动 HideWG：**

```bash
cd /opt/hidewg
./hidewg run --role server --config configs/server.yaml
```

终端输出 `[server]` 开头的日志，等待客户端连接。

**步骤 2 — 客户端启动 HideWG：**

```powershell
cd E:\safewg
.\hidewg.cmd run --role client --config configs/client.yaml
```

终端输出 `[client]` 开头的日志。此时两端的 HideWG 代理已经在监听，但 WireGuard 尚未启动，不会有实际流量。

**步骤 3 — 服务端启动 WireGuard：**

```bash
# 新开一个终端
sudo wg-quick up wg0
```

输出应显示 `[#] wg setconf wg0 /dev/fd/63` 和接口配置信息。

**步骤 4 — 客户端激活 WireGuard：**

在 WireGuard Windows 客户端中，点击 `HideWG-VPN` 隧道旁边的 **"激活"** 按钮。

激活后 WireGuard 界面应显示：
- **状态：** 已连接
- **发送/接收：** 字节数开始增长（keepalive 流量）
- **最近握手：** 显示时间戳

**步骤 5 — 验证连通性：**

```powershell
# 客户端 Windows PowerShell
ping 10.66.0.1
```

预期输出：

```
正在 Ping 10.66.0.1 具有 32 字节的数据:
来自 10.66.0.1 的回复: 字节=32 时间<1ms TTL=64
```

#### 停止流程（逆序）

```
步骤 1    客户端断开 WireGuard
  ↓
步骤 2    服务端停止 WireGuard
  ↓
步骤 3    客户端停止 HideWG
  ↓
步骤 4    服务端停止 HideWG
```

```bash
# 客户端：WireGuard 界面点击 "断开"

# 服务端：停止 WireGuard
sudo wg-quick down wg0

# 客户端：停止 HideWG（Ctrl+C 或）
.\hidewg.cmd stop --role client

# 服务端：停止 HideWG
./hidewg stop --role server
```

---

### 2.6 服务端后台运行（systemd）

前台运行会在终端关闭后退出。使用 systemd 将 HideWG 注册为系统服务。

**创建服务文件：**

```bash
sudo vim /etc/systemd/system/hidewg-server.service
```

写入：

```ini
[Unit]
Description=HideWG Server Proxy
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
WorkingDirectory=/opt/hidewg
ExecStart=/usr/bin/python3 /opt/hidewg/hidewg run --role server --config configs/server.yaml
Restart=on-failure
RestartSec=5
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target
```

**启用并启动：**

```bash
sudo systemctl daemon-reload
sudo systemctl enable hidewg-server
sudo systemctl start hidewg-server

# 查看状态
sudo systemctl status hidewg-server

# 查看日志
sudo journalctl -u hidewg-server -f
```

**WireGuard 也注册为开机自启：**

```bash
sudo systemctl enable wg-quick@wg0
```

这样重启服务器后，HideWG 和 WireGuard 会自动按正确顺序启动。

---

### 2.7 本地回环测试（单机部署）

如果只有一台机器，可以在同一台机器上测试完整链路。

**使用配置文件：** `configs/real-server.yaml` 和 `configs/real-client.yaml`

所有端口都绑定 `127.0.0.1`，模拟两台机器的通信。

**终端 1 — 启动服务端 HideWG：**

```bash
./hidewg run --role server --config configs/real-server.yaml
```

**终端 2 — 启动客户端 HideWG：**

```bash
./hidewg run --role client --config configs/real-client.yaml
```

**终端 3 — 启动服务端 WireGuard：**

```bash
sudo wg-quick up wg0   # 使用 wg-server.conf.example 配置
```

**终端 4 — 启动客户端 WireGuard：**

```bash
sudo wg-quick up wg0   # 使用 wg-client.conf.example 配置
```

**验证：**

```bash
ping 10.66.0.1
curl http://10.66.0.1   # 如果服务端运行了 HTTP 服务
```

---

### 2.8 部署验证清单

部署完成后，逐项确认：

| 检查项 | 命令 | 预期结果 |
|--------|------|---------|
| HideWG 进程运行 | `ps aux \| grep hidewg` 或 `systemctl status hidewg-server` | 进程存在 |
| PID 文件存在 | `cat .hidewg/server.pid` | 输出 PID 数字 |
| 状态文件正常 | `cat .hidewg/server_state.json` | JSON 格式，`session_state` 为 RUNNING |
| 包计数增长 | 反复查看 `server_state.json` 中的 `inner_packets_sent` | 数值递增 |
| WireGuard 握手成功 | `sudo wg show` | `latest handshake` 显示时间 |
| VPN 内网互通 | `ping 10.66.0.1` | 有回复 |
| 外层端口不可识别 | 用 Wireshark 抓包查看外层端口流量 | 首字节随机，无 WireGuard 特征 |
| 防火墙正确 | `sudo ufw status` | 仅放通外层 UDP 端口 |

---

### 2.9 常见部署问题

#### WireGuard 握手超时

**症状**：`sudo wg show` 中 `latest handshake` 始终为空，`transfer` 显示 0B。

**排查步骤**：

```bash
# 1. 确认 HideWG 代理在运行且端口监听中
sudo ss -ulnp | grep -E '55821|51831'
# 应看到两个进程分别监听 55821（外层）和 51831（内层）

# 2. 检查对端地址是否学习成功
cat .hidewg/server_state.json | python3 -m json.tool | head -5
# 如果 outer_packets_received > 0 说明外层代理收到了对端数据

# 3. 检查内层转发是否正常
cat .hidewg/server_state.json | grep inner_packets
# inner_packets_sent 应该 > 0（HideWG 把解码后的包转发给 WireGuard）

# 4. 用 tcpdump 抓包确认
sudo tcpdump -i lo -n udp port 51821 -c 10
# 应看到 127.0.0.1:51831 → 127.0.0.1:51821 的 UDP 包
```

**常见原因**：
- 启动顺序错误：WireGuard 先于 HideWG 启动，握手包发到了空端口
- `shared_secret` 不一致：外层 HMAC 验证失败
- `peer_host` 填错：外层 UDP 包发到了错误的地址
- 防火墙拦截了外层端口

#### NAT 穿透失败

**症状**：客户端在 NAT 后面，服务端无法直接向客户端发包。

**原因**：HideWG 外层使用 UDP，NAT 设备会丢弃未经请求的入站 UDP 包。

**解决方案**：

1. 确保客户端配置了 `timing_jitter_ms`（不为 0），使客户端定期主动发包，维持 NAT 映射
2. 客户端配置 `PersistentKeepalive: 25`（WireGuard 层面保活）
3. 如果 NAT 严格限制端口映射，考虑在服务端也设置较低的 `timing_jitter_ms` 以增加客户端→服务端的发包频率

#### 包计数不增长

**症状**：`server_state.json` 中所有计数器始终为 0。

**排查**：

```bash
# 确认外层端口在监听
ss -ulnp | grep 55821

# 从客户端发一个测试 UDP 包
echo "test" | nc -u <服务器IP> 55821

# 服务端检查是否有数据到达
cat .hidewg/server_runtime.jsonl | tail -3
```

如果 `outer_packets_received` 仍为 0，检查客户端 `peer_host` 是否正确指向服务器公网 IP。

#### 认证失败持续增加

**症状**：`authentication_failures` 数值持续增长。

**原因**：两端 `shared_secret` 不一致，或 `session_id` 不一致。

```bash
# 对比两端配置
diff <(grep shared_secret configs/client.yaml) <(grep shared_secret configs/server.yaml)
diff <(grep session_id configs/client.yaml) <(grep session_id configs/server.yaml)
```

#### Windows 客户端防火墙

**症状**：服务端能收到客户端的包，但客户端收不到服务端的回复。

**解决**：Windows Defender 防火墙可能拦截入站 UDP。

```powershell
# PowerShell（管理员）
New-NetFirewallRule -DisplayName "HideWG Outer" -Direction Inbound -Protocol UDP -LocalPort 55820 -Action Allow
```

---

## 第三阶段：流量分析（Wireshark）

### 3.1 抓取外层流量

验证工具会自动生成 PCAP 文件，也可以用 Wireshark 实时抓包。

**自动生成的 PCAP：**

- `artifacts/raw_wireguard.pcap`：原始 WireGuard 流量，可以看到固定头部特征（首字节 01/02/03/04）
- `artifacts/capture.pcap`：HideWG 封装后的流量，看不到任何 WireGuard 特征

### 3.2 对比分析

在 Wireshark 中打开两个 PCAP 文件进行对比：

**原始 WireGuard 流量特征：**
- 首字节固定为 01(握手发起)、02(握手响应)、03(数据)、04(keepalive)
- 第 2-4 字节全零（reserved 字段）
- 握手包固定长度：发起 148 字节，响应 92 字段
- keepalive 包固定 32 字节

**HideWG 封装后：**
- 所有外层包首字节随机，无固定模式
- 包长度经过自适应桶填充 + 高斯抖动（`bucket_jitter_sigma: 15.0`），精确桶对齐被打破
- S4 随机前填充（0~48 字节）+ 噪声前缀（0~24 字节）两层随机偏移，进一步混淆包长
- 碎片大小随机化（`min_fragment_payload ~ max_fragment_payload`），无等长碎片模式
- 掩护流量（泊松过程）填充空闲期，双向模拟正常 Web 应用
- 握手时序特征被掩盖，keepalive 模式不可辨识
- 使用 `padding_mode: continuous` 时，包长从多峰高斯混合（模拟 QUIC/DNS/VoIP）连续采样，完全消除桶边界特征

---

## 常见问题

### Q：验证时提示 `passed: false`

检查 `artifacts/verify_report.json` 中 `function_tests` 字段，找到具体失败的测试项。常见原因：

- 端口被占用：验证测试会动态分配 UDP 端口，如果系统端口耗尽可能失败
- Python 版本过低：需要 Python 3.10+

### Q：真实部署时 WireGuard 握手不成功

1. 确认启动顺序：先启动 HideWG 代理，再启动 WireGuard
2. 检查防火墙是否放通了外层代理端口（默认 55820/55821）
3. 确认两端 `shared_secret` 一致
4. 查看 `.hidewg/client_state.json` 和 `.hidewg/server_state.json` 确认包计数是否增长

### Q：查看代理运行状态

```bash
./hidewg status --output state.json
cat state.json
```

输出包含：运行时长、内外层包计数、字节统计、分片数、认证失败数、重放丢弃数、实时吞吐和延迟估算。

### Q：自定义填充策略

在配置文件中调整：

```yaml
padding_buckets: [96, 128, 192, 256, 384, 512, 768, 1024, 1280]  # 长度桶
padding_jitter_chance: 0.20   # 20% 概率跳到下一个桶
timing_jitter_ms: 2.5         # 每包随机延迟 0~2.5ms
```

增大桶数量和 jitter 可以提升隐蔽性，但会增加带宽开销。
