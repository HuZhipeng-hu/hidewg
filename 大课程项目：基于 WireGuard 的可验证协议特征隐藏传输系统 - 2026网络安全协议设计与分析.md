# 大课程项目：基于 WireGuard 的可验证协议特征隐藏传输系统 - 2026网络安全协议设计与分析



## 一、前置知识：WireGuard 为什么容易被识别？

本项目的背景是：WireGuard 本身是一个设计简洁、高效、安全的 VPN 协议，但它**并不以协议混淆或流量伪装为目标**。WireGuard 官方说明中也明确提到，WireGuard 不专注于 obfuscation，若需要混淆，应在 WireGuard 之上的另一层完成。([WireGuard](https://www.wireguard.com/known-limitations/?utm_source=chatgpt.com))

WireGuard 的现有流量主要有以下可识别特征。

### 1. 明确运行在 UDP 之上

WireGuard 报文通常表现为 UDP 流量。外部观察者虽然看不到加密后的内部 IP 包内容，但可以观察到：

```text
UDP 五元组
UDP payload 长度
包方向
包到达时间
包长分布
会话启动时序
```

这些元数据本身就可能形成协议指纹。

------

### 2. 报文开头存在固定结构

WireGuard 协议报文中包含 `message_type` 和保留字段。官方协议文档中，握手发起报文 `handshake_initiation` 的开头字段为：

```text
u8 message_type
u8 reserved_zero[3]
u32 sender_index
...
```

其中 `message_type = 1`，后面 3 个保留字节为 0。([WireGuard](https://www.wireguard.com/protocol/?utm_source=chatgpt.com))

类似地，WireGuard 不同类型的协议消息都有固定的类型字段：

| message_type | 含义                 |
| ------------ | -------------------- |
| 1            | Handshake Initiation |
| 2            | Handshake Response   |
| 3            | Cookie Reply         |
| 4            | Transport Data       |

这意味着，外部观察者可以通过 UDP payload 的前几个字节做启发式判断。

Wireshark 对 WireGuard 的捕获过滤规则也体现了这一点：它会检查 UDP payload 的第一个字节是否在 1 到 4 之间，并检查后面 3 个字节是否为 0。

------

### 3. 握手包长度和时序比较稳定

WireGuard 的握手消息字段结构比较固定，因此握手阶段的包长和方向序列具有明显特征。例如：

```text
客户端 → 服务器：Handshake Initiation
服务器 → 客户端：Handshake Response
之后进入加密数据传输
```

即使内部内容被加密，外部仍然可以看到：

```text
启动阶段的包长
启动阶段的方向变化
握手后数据包的连续传输模式
```

------

### 4. 数据包中存在计数器等固定字段位置

WireGuard 的传输数据包中包含 `counter` 字段，Wireshark 的 WireGuard 显示字段中也包含 `wg.counter`、`wg.encrypted_packet`、`wg.encrypted_empty` 等字段。

因此，WireGuard 的识别不仅可以依靠单个包的字节结构，还可以依靠一段流量中的统计规律。

------

### 5. 本项目要解决的问题

> 能否在不改变 WireGuard 核心安全协议的前提下，在其外侧增加一层新的传输封装，使外部观察者更难直接识别 WireGuard 流量，同时尽量保持较高传输效率？

------

## 二、项目总体目标

每个小组需要实现一个系统，暂称为 **HideWG**。

系统目标是：

```text
原始应用流量
    ↓
WireGuard 隧道
    ↓
HideWG 外层封装
    ↓
网络传输
    ↓
HideWG 解封装
    ↓
WireGuard 隧道
    ↓
目标应用
```

也就是说，WireGuard 仍然负责核心 VPN 安全功能，学生主要开发的是：

```text
WireGuard 外侧的协议特征隐藏层
```

项目的最终比较重点是两类指标：

1. **隐匿性**：外部观察者是否还能容易识别这是 WireGuard 流量；
2. **效率**：增加隐藏层后，吞吐、延迟、CPU、带宽开销是否仍然可接受。

------

## 三、项目边界

### 必须遵守

学生必须：

1. 使用 WireGuard 作为内层安全隧道；
2. 在 WireGuard 外侧实现一层新的封装协议；
3. 保证端到端网络功能可用；
4. 实现统一验证接口；
5. 提交隐匿性评估结果；
6. 提交效率评估结果；
7. 能解释系统内部核心逻辑。

### 不允许

学生不得：

1. 修改 WireGuard 核心密码协议；
2. 只做一个能联网的外层 Demo；
3. 只展示网页访问或 ping 通；
4. 使用现成混淆工具简单包装后提交；
5. 抓取或测试未授权真实网络环境；
6. 伪造评测数据。

------

# 四、系统模块划分

整个系统建议划分为 7 个核心模块。课程任务书只规定模块边界和功能要求，具体策略由各组自行设计。

------

## 模块 1：WireGuard 基线环境模块

### 功能

负责搭建原始 WireGuard 隧道，作为后续评估的基线。

### 输入

```text
WireGuard 配置文件
客户端节点
服务器节点
测试流量
```

### 输出

```text
原始 WireGuard 可用隧道
原始 WireGuard 抓包数据
原始 WireGuard 性能数据
```

### 要求

每组必须先证明：

```text
不加 HideWG 时，原始 WireGuard 能正常工作。
```

这是后续所有比较的基准。

------

## 模块 2：WireGuard 适配模块

### 功能

负责接收本地 WireGuard 发出的 UDP 报文，并交给 HideWG 封装层。

### 位置

```text
WireGuard UDP 报文
    ↓
WG Adapter
    ↓
HideWG 封装模块
```

### 要求

该模块需要完成：

```text
监听本地 UDP 端口
接收 WireGuard 报文
转交给 HideWG 编码模块
接收解封装后的 WireGuard 报文
转发回本地 WireGuard 端口
```

这个模块的边界要清楚：它只负责“接入 WireGuard”，不负责隐藏策略。

------

## 模块 3：HideWG 协议封装模块

### 功能

这是系统核心模块之一，负责把原始 WireGuard 报文封装成新的外层传输格式。

### 基本要求

每个小组需要自行设计一种外层记录格式，例如：

```text
HideWG Record
- session_id
- sequence_number
- payload_length
- encrypted_payload
- authentication_tag
- optional_padding
```

但具体字段如何安排、哪些字段明文、哪些字段加密，由各组自行决定。

### 设计目标

该模块需要降低以下特征暴露：

```text
WireGuard message_type 暴露
固定保留字段暴露
固定握手包长度暴露
明显的数据包长度分布
明显的启动时序
明显的 keepalive 模式
```

------

## 模块 4：隐藏策略模块

### 功能

隐藏策略模块决定如何让封装后的流量“不像原始 WireGuard”。

### 允许各组自由设计的策略

例如：

| 策略方向   | 说明                                 |
| ---------- | ------------------------------------ |
| 头部隐藏   | 不让外部直接看到 WireGuard 报文开头  |
| 长度填充   | 把包长填充到若干长度桶               |
| 分片       | 把大包拆成多个小包                   |
| 合并       | 把多个小包合并发送                   |
| 时序扰动   | 对部分包做轻微延迟                   |
| 随机化     | 对外层格式、长度、发送模式进行随机化 |
| 多策略组合 | 根据流量类型动态选择策略             |

不同小组可以自由发挥，并通过最终指标竞争。

------

## 模块 5：解封装与恢复模块

### 功能

服务器端收到 HideWG 报文后，需要恢复出原始 WireGuard UDP 报文。

### 要求

必须支持：

```text
解码外层记录
验证完整性
去除填充
处理分片重组
处理乱序
丢弃非法报文
恢复原始 WireGuard 报文
转发给 WireGuard
```

这个模块的核心要求是：

```text
隐藏层不能破坏 WireGuard 原有功能。
```

------

## 模块 6：状态与日志模块

### 功能

用于展示系统内部是否真实工作，而不是只看外层功能。

### 必须输出的状态信息

每组至少需要输出：

```text
当前会话状态
已发送内层包数量
已接收内层包数量
已发送外层包数量
已接收外层包数量
填充字节数
分片数量
重组失败次数
认证失败次数
重放丢弃次数
当前吞吐
当前延迟估计
```

### 目的

验收时检查：

```text
WireGuard 内层包数量是否与 HideWG 外层包数量对应？
填充是否真的发生？
分片是否真的发生？
异常包是否真的被丢弃？
```

------

## 模块 7：统一验证模块

### 功能

这是所有小组必须遵守的统一验收入口。

每组可以内部实现不同，但必须提供统一命令：

```bash
./hidewg verify
```

该命令需要自动完成：

```text
启动测试环境
建立 WireGuard 隧道
启动 HideWG
执行功能测试
执行隐匿性测试
执行效率测试
输出统一报告
```

最终输出：

```text
verify_report.json
stealth_report.json
performance_report.csv
runtime_log.jsonl
capture.pcap
```

------

# 五、统一验证接口

为了公平 PK，每组最终必须支持统一命令。

## 1. 启动系统

```bash
./hidewg run --role client --config client.yaml
./hidewg run --role server --config server.yaml
```

## 2. 执行统一验证

```bash
./hidewg verify --config lab.yaml --output artifacts/
```

## 3. 导出状态

```bash
./hidewg status --output state.json
```

## 4. 停止系统

```bash
./hidewg stop
```

------

# 六、验证指标体系

最终比较主要看两大类指标：

```text
隐匿性
效率
```

但在评分时还需要先检查功能正确性。也就是说：

> 功能不正确的系统，不能参与隐匿性和效率排名。

------

## A. 功能正确性指标

这是准入门槛。

| 指标       | 要求                                 |
| ---------- | ------------------------------------ |
| 基础连通性 | 隧道内 ping 成功                     |
| TCP 可用性 | 能通过隧道传输文件                   |
| UDP 可用性 | 能通过隧道发送 UDP 流                |
| 文件完整性 | 传输前后 SHA256 一致                 |
| 大包处理   | 不因 MTU 或分片问题崩溃              |
| 异常包处理 | 非法、重复、篡改报文不能导致系统崩溃 |
| 状态可观测 | 能输出系统内部状态                   |

功能正确性建议只判定：

```text
通过 / 不通过
```

通过后才进入 PK 排名。

------

## B. 隐匿性指标

隐匿性评价回答的问题是：

> 外部观察者还能不能通过规则或分类算法识别出这是 WireGuard 流量？

隐匿性建议分成两层。

------

### B1. 规则检测

规则检测器用于检测明显 WireGuard 特征。

最低要求检测：

| 规则 | 检测内容                                          |
| ---- | ------------------------------------------------- |
| R1   | UDP payload 首字节是否为 1、2、3、4               |
| R2   | 后 3 个字节是否为 0                               |
| R3   | 是否出现典型 WireGuard 握手包长度                 |
| R4   | 是否出现明显 handshake initiation → response 时序 |
| R5   | 是否出现明显固定长度 keepalive 模式               |

输出指标：

```text
Rule Hit Rate
```

即规则命中率。越低越好。

------

### B2. 分类算法检测

每组需要构建一个简单分类器，用于判断流量属于哪一类：

```text
原始 WireGuard 流量
HideWG 流量
普通对照流量（各小组自行考虑）
```

可使用以下的深度学习模型（可扩充），重点是特征提取和实验可复现：

```text
Logistic Regression
Random Forest
SVM
KNN
```

### 最低特征集合

分类器至少使用以下特征：

| 特征类型     | 示例                                               |
| ------------ | -------------------------------------------------- |
| 包长特征     | 平均包长、最大包长、最小包长、包长方差、包长直方图 |
| 方向特征     | 上下行包数量比例、上下行字节比例                   |
| 时间特征     | 包间隔均值、包间隔方差、P95 包间隔                 |
| burst 特征   | 连续同方向包数量、burst 长度                       |
| 启动阶段特征 | 前 32/64/128 个包的长度和方向序列                  |

输出指标：

```text
Accuracy
Precision
Recall
F1
AUC
Confusion Matrix
```

对于 HideWG 来说，如果分类器很容易把它识别出来，说明隐匿性不好。

------

### B3. 隐匿性指标建议

| 子指标           | 含义                         |
| ---------------- | ---------------------------- |
| 规则规避能力     | 是否避开 WireGuard 显式规则  |
| 分类器规避能力   | 是否不容易被统计分类器识别   |
| 流量分布接近程度 | 是否不像一个新的固定协议指纹 |

隐匿性分数越高越好。

------

## C. 效率指标

效率评价回答的问题是：

> 加了隐藏层以后，系统还能不能高效工作？

最低测试指标如下。

| 指标         | 含义                                   |
| ------------ | -------------------------------------- |
| 吞吐量保持率 | HideWG 吞吐量 / 原始 WireGuard 吞吐量  |
| 延迟增加     | HideWG RTT - 原始 WireGuard RTT        |
| 带宽膨胀率   | 外层传输字节数 / 原始 WireGuard 字节数 |
| CPU 开销     | HideWG 进程 CPU 使用率                 |
| 内存开销     | HideWG 进程内存占用                    |
| 丢包环境表现 | 在 1%、3%、5% 丢包下是否稳定           |
| 并发能力     | 多连接同时传输是否稳定                 |



# 七、建议的课程项目模块图

```text
                   ┌──────────────────────┐
                   │   测试应用流量        │
                   │ ping / HTTP / UDP     │
                   └──────────┬───────────┘
                              ↓
                   ┌──────────────────────┐
                   │   WireGuard 隧道      │
                   │   原始安全传输层      │
                   └──────────┬───────────┘
                              ↓
                   ┌──────────────────────┐
                   │   WG 适配模块         │
                   │ 接收/转发 WG UDP 包   │
                   └──────────┬───────────┘
                              ↓
        ┌────────────────────────────────────────┐
        │          HideWG 核心系统                │
        │                                        │
        │  ┌────────────┐  ┌────────────┐        │
        │  │ 协议封装   │  │ 隐藏策略   │        │
        │  └────────────┘  └────────────┘        │
        │  ┌────────────┐  ┌────────────┐        │
        │  │ 会话管理   │  │ 解封装恢复 │        │
        │  └────────────┘  └────────────┘        │
        └────────────────────────────────────────┘
                              ↓
                   ┌──────────────────────┐
                   │   外部 UDP 网络       │
                   └──────────┬───────────┘
                              ↓
                   ┌──────────────────────┐
                   │  对端 HideWG          │
                   └──────────┬───────────┘
                              ↓
                   ┌──────────────────────┐
                   │  对端 WireGuard       │
                   └──────────────────────┘


统一验证体系：

┌─────────────────────┐
│ 功能正确性验证       │
└─────────────────────┘
┌─────────────────────┐
│ 隐匿性验证           │
│ 规则检测 + 分类检测  │
└─────────────────────┘
┌─────────────────────┐
│ 效率验证             │
│ 吞吐 / 延迟 / 开销   │
└─────────────────────┘
```

------

# 八、需要完成的核心交付物

每组最终提交：

| 类型         | 内容                          |
| ------------ | ----------------------------- |
| 系统代码     | HideWG 客户端与服务器         |
| 配置文件     | WireGuard 配置、HideWG 配置   |
| 运行脚本     | 一键启动、一键停止、一键验证  |
| 验证报告     | 功能、隐匿性、效率            |
| 抓包文件     | 原始 WireGuard 和 HideWG 流量 |
| 分类器代码   | 特征提取与分类算法            |
| 性能测试代码 | 吞吐、延迟、CPU、带宽开销     |
| 设计文档     | 模块设计、策略说明、关键决策  |
| 答辩材料     | 系统演示、指标对比、问题分析  |

------

