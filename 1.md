## WireGuard 协议封装流程

HideWG 的核心设计原则是**不修改 WireGuard 本身**，所有封装发生在 WireGuard 隧道的外层。下面按数据流向逐层拆解。

------

### 整体架构



```
WireGuard (UDP:51820)
       │
       ▼
  HideWG 代理 (UDP:51830)
       │
       ▼  原始 WireGuard UDP 包（明文 WireGuard 帧）
  HideWG 编码器 (HideWGCodec)
       │
       ▼  HideWG Record（ChaCha20Poly1305 加密）
  WebSocket 帧封装 (ws_encode_frame)
       │
       ▼  WS Binary Frame
  TLS 1.3 加密 (nginx / OpenSSL)
       │
       ▼  HTTPS 流量（DPI 无法识别）
  网络传输 → 服务器:443
```

------

### 第一步：捕获 WireGuard UDP 包

[adapter.py:249-254](vscode-webview://0g2bjccm0r6nrvnit3dhve9aq02j0lkj0daad1ekpifo9hcu3icr/hidewg_app/adapter.py#L249-L254)：



```python
def _handle_inner(self, inner_sock: socket.socket, outer_transport) -> None:
    data, addr = inner_sock.recvfrom(65535)  # 原始 WireGuard UDP 包
    self.learned_inner_addr = addr
    records = self.codec.encode_packet(data)  # 交给编码器
```

WireGuard 绑定在 `127.0.0.1:51820`，HideWG 代理监听 `127.0.0.1:51830`。WireGuard 的 peer endpoint 配置为 `127.0.0.1:51830`，所以 WireGuard 发出的 UDP 包被 HideWG 代理截获。

此时 `data` 是一个**完整的 WireGuard UDP 包**，格式可能是：

- 握手发起：`0x01` + 保留零字节 + 公钥 + MAC1 + MAC2（148B）
- 握手响应：`0x02` + 保留零字节 + 公钥 + MAC1 + MAC2（92B）
- 数据传输：`0x04` + 计数器 + 加密数据（可变长）
- 心跳：`0x04` + 计数器（32B）

------

### 第二步：HideWG 编码（核心封装）

[protocol.py:301-338](vscode-webview://0g2bjccm0r6nrvnit3dhve9aq02j0lkj0daad1ekpifo9hcu3icr/hidewg_app/protocol.py#L301-L338)：

#### 2.1 随机分片



```python
chunks = self._random_split(payload) or [b""]
```

如果 WireGuard 包 > 1300B，拆成**不等长**的随机片段（434B ~ 1300B）。

#### 2.2 填充到目标包长



```python
padded_len = self.padding_policy.padded_length(len(chunk))
padding_len = padded_len - len(chunk)
body_plain = chunk + os.urandom(padding_len)
```

TrafficMimicry 策略从 ~60 种真实 HTTPS 包长中加权采样目标长度，不足部分用随机字节填充。

#### 2.3 构造加密头部



```python
HEADER = struct.Struct("!BBIQIHHHH")  # 26 字节

header_plain = HEADER.pack(
    VERSION,           # B: 协议版本 (1)
    flags,             # B: 分片标志
    self.session_id,   # I: 会话 ID (0x48445747)
    sequence,          # Q: 64位序列号
    message_id,        # I: 32位消息 ID
    fragment_index,    # H: 当前分片索引
    len(chunks),       # H: 总分片数
    len(chunk),        # H: 原始 payload 长度
    padding_len,       # H: 填充长度
)
```

头部 26 字节，包含所有重组和校验所需信息。

#### 2.4 AEAD 加密



```python
nonce = self._nonce()                           # 12 字节随机 nonce
plaintext = header_plain + body_plain            # 头部 + body
ct_tag = self._aead.encrypt(nonce, plaintext, b"")  # ChaCha20Poly1305
```

单次 AEAD 调用同时完成：

- **加密**：header + body 全部加密，WireGuard 的 `0x01/0x02/0x03/0x04` 首字节、保留零字节全部消失
- **认证**：生成 16 字节认证标签，防篡改

#### 2.5 最终 Record 格式



```python
data = nonce + ct_tag
# 结构: nonce[12] || encrypted_header[26] || encrypted_body[...] || tag[16]
```

| 字段             | 长度 | 说明                         |
| ---------------- | ---- | ---------------------------- |
| nonce            | 12B  | 随机 nonce，保证每次加密唯一 |
| encrypted_header | 26B  | 加密后的协议头               |
| encrypted_body   | 可变 | 加密后的 WireGuard 包 + 填充 |
| tag              | 16B  | ChaCha20Poly1305 认证标签    |

**关键点**：外部观察者看到的只是随机字节流，无法区分头部和 body，更无法看到 WireGuard 的任何特征。

------

### 第三层：WebSocket 帧封装

[ws.py:37-55](vscode-webview://0g2bjccm0r6nrvnit3dhve9aq02j0lkj0daad1ekpifo9hcu3icr/hidewg_app/ws.py#L37-L55)：



```python
def ws_encode_frame(opcode: int, payload: bytes, masked: bool = True) -> bytes:
    header = bytes([WS_FIN | opcode])           # 0x82 (FIN + Binary)
    # 客户端: masked=True, 服务端: masked=False
    if length < 126:
        header += bytes([mask_bit | length])
    elif length < 65536:
        header += bytes([mask_bit | 126]) + struct.pack("!H", length)
    # 客户端添加 4 字节随机 mask key
    if masked:
        mask_key = os.urandom(4)
        masked_payload = payload[i] ^ mask_key[i % 4]
        return header + mask_key + bytes(masked_payload)
```

HideWG Record 被封装为 WS Binary Frame：



```
[FIN+0x02][Mask+Len][Extended Len?][Masking Key?][HideWG Record]
```

从 DPI 视角看，这是一个标准的 WebSocket 二进制数据帧，与聊天应用、实时推送的流量完全一致。

------

### 第四层：TLS 1.3 + nginx

客户端侧（[client.yaml:58-60](vscode-webview://0g2bjccm0r6nrvnit3dhve9aq02j0lkj0daad1ekpifo9hcu3icr/configs/client.yaml#L58-L60)）：



```
客户端 → WSS (TLS 1.3) → nginx:443
```

nginx 配置：



```nginx
server {
    listen 443 ssl;
    ssl_protocols TLSv1.3;
    location /ws {
        proxy_pass http://127.0.0.1:8443;  # 转发到 HideWG 服务端
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}
```

nginx 做 TLS 终止 + WebSocket 反代，DPI 在网络上看到的是标准 HTTPS 握手。

------

### 接收端解封装（反向流程）

[adapter.py:381-409](vscode-webview://0g2bjccm0r6nrvnit3dhve9aq02j0lkj0daad1ekpifo9hcu3icr/hidewg_app/adapter.py#L381-L409)：



```python
def _handle_outer(self, outer_transport, inner_sock):
    data, addr = outer_transport.recv()          # 1. TLS 解密 → WS 帧
    data = self.junk_injector.strip_noise(data)  # 2. 剥离噪声前缀
    fragment = self.codec.decode_record(data)    # 3. AEAD 解密 → 头部 + body
    packet, status = self.reassembler.accept(fragment)  # 4. 分片重组
    inner_sock.sendto(packet, target)            # 5. 还原为原始 WireGuard UDP 包
```

`decode_record` ([protocol.py:340-385](vscode-webview://0g2bjccm0r6nrvnit3dhve9aq02j0lkj0daad1ekpifo9hcu3icr/hidewg_app/protocol.py#L340-L385))：



```python
nonce = record[:12]
ct_tag = record[12:]
plaintext = self._aead.decrypt(nonce, ct_tag, b"")  # AEAD 解密 + 认证
header_plain = plaintext[:26]                         # 提取头部
body_plain = plaintext[26:]                           # 提取 body
# 从头部读取 payload_len，截取原始 WireGuard 包
return body_plain[:payload_len]
```

------

### 完整数据流图



```
发送端 WireGuard:51820
    │  UDP 包 (如 148B 握手帧: 01 00 00 00 ...)
    ▼
HideWG Codec.encode_packet()
    ├─ _random_split()          → 随机分片
    ├─ padded_length()          → TrafficMimicry 填充到 ~1400B
    ├─ HEADER.pack()            → 26B 明文头部
    ├─ AEAD.encrypt()           → ChaCha20Poly1305 全加密
    └─ 输出: nonce[12] + 密文 + tag[16]
    ▼
ws_encode_frame()
    └─ 封装为 WS Binary Frame (masked)
    ▼
TLS 1.3 加密
    ▼
HTTPS 流量 (DPI 看到: TLS Client Hello → WS Upgrade → 加密数据)
    ════════════════════════════════════════════════════
    ▼
nginx:443 TLS 终止 → WS 反代 → HideWG:8443
    ▼
WS 帧解码 → strip_noise() → AEAD 解密 → 重组
    ▼
还原 WireGuard UDP 包 → 转发给 WireGuard:51820
```